"""macOS (beta) platform backends: Quartz keystroke injection and a
listen-only event-tap hotkey listener. Selected automatically by the
create_injector()/create_hotkey_listener() factories on darwin."""
